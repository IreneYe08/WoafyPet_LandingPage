# Archive: Definitely unused files

These files were moved here from the main source tree because they are not imported or referenced anywhere (see project root `UNUSED_FILES_SCAN_REPORT.md`).

- **CheckoutPage.tsx** — Replaced by Stripe payment link flow; never imported.
- **Countdown.tsx** — Unused; MiniCountdown and PersistentCountdown implement their own logic.

Folder structure under `_archive_unused_files` mirrors the original `src/` paths. Nothing here is built or executed; files are kept for reference only.
